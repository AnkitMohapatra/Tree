int count(node *root)
{
 node *temp = root;
 int c = 0;
 if(temp==NULL)
   return 0;
 else
 {
  while(temp->left != NULL)
  {
   c++;
   temp = temp->left;
  }
 temp = root;
   while(temp->right != NULL)
   {
    c++;
    temp = temp->right;
   }
 }
cout << c+1;
}
